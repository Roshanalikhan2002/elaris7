<div class="social-grid-wrapper">
  <div class="social-header">
    <h2 class="social-title">{{ section.settings.title | default: 'elaris7 + you' }}</h2>
    <a href="{{ section.settings.button_link | default: '#' }}" class="btn-pill-hollow-social">FIND US ON SOCIAL</a>
  </div>

  <div class="social-grid-container">
    {% for i in (1..5) %}
      <div class="social-item">
        {% assign img_key = 'image_' | append: i %}
        {% if section.settings[img_key] != blank %}
          {{ section.settings[img_key] | image_url: width: 800 | image_tag: class: 'social-img', loading: 'lazy' }}
        {% else %}
           {% comment %} User request: Pick images L to R (1,2,3,4) and place R to L (5,4,3,2) skipping 1st {% endcomment %}
           {% if i == 5 %}
             <img src="{{ 'social-face.jpg' | asset_url }}" alt="Model Photo" class="social-img">
           {% elsif i == 4 %}
             <img src="{{ 'social-jar.jpg' | asset_url }}" alt="Product Close-up" class="social-img">
           {% elsif i == 3 %}
             <img src="{{ 'social-anti-acne.png' | asset_url }}" alt="Anti Acne" class="social-img">
           {% elsif i == 2 %}
             <img src="{{ 'social-hand.jpg' | asset_url }}" alt="Brand Aesthetic" class="social-img">
           {% else %}
             <div class="placeholder-social">User Story</div>
           {% endif %}
        {% endif %}
      </div>
    {% endfor %}
  </div>
</div>

<style>
.social-grid-wrapper {
  margin: 60px 12px;
  background-color: #f7f6f2; /* Light creamy background like the ref */
  padding: 60px 40px;
  border-radius: 14px;
}

.social-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 40px;
}

.social-title {
  font-size: 2rem;
  font-weight: 800;
  letter-spacing: -0.02em;
  color: #333;
}

.btn-pill-hollow-social {
  padding: 12px 30px;
  border: 1px solid #333;
  border-radius: 50px;
  font-size: 0.7rem;
  font-weight: 700;
  color: #333;
  text-decoration: none;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  transition: all 0.3s ease;
}

.btn-pill-hollow-social:hover {
  background: #333;
  color: #fff;
}

.social-grid-container {
  display: flex;
  gap: 16px;
  overflow-x: auto;
  scrollbar-width: none; /* Hide scrollbar for clean look */
  -ms-overflow-style: none; /* Internet Explorer and Edge */
}

.social-grid-container::-webkit-scrollbar {
  display: none; /* Chrome, Safari, and Opera */
}

.social-item {
  flex: 0 0 calc(25% - 12px); /* ensure exactly 4 items are shown by default on desktop */
  border-radius: 12px;
  overflow: hidden;
  aspect-ratio: 1 / 1;
}

.social-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.placeholder-social {
  width: 100%;
  height: 100%;
  background: #e0deda;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.8rem;
  color: #777;
  text-align: center;
  padding: 10px;
}

@media (max-width: 768px) {
  .social-grid-wrapper {
    margin: 30px 0 0;
    padding: 40px 20px;
    border-radius: 14px 14px 0 0; /* Only top rounding as it is likely at the bottom */
  }
  .social-title {
    font-size: 1.6rem;
  }
  .social-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 20px;
  }
  .social-grid-container {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    gap: 12px;
    margin: 0 -20px; /* Bleed to edges */
    padding: 0 20px 10px;
    scrollbar-width: none; /* Hide scrollbar for clean look */
  }
  .social-grid-container::-webkit-scrollbar {
    display: none;
  }
  .social-item {
    flex: 0 0 75%; /* Large mobile cards like the ref */
    scroll-snap-align: start;
    aspect-ratio: 1 / 1.1; /* Slightly taller for mobile impact */
  }
}
</style>

{% schema %}
{
  "name": "Elaris7 + You Social",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "elaris7 + you"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "image_picker",
      "id": "image_1",
      "label": "Photo 1"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "Photo 2"
    },
    {
      "type": "image_picker",
      "id": "image_3",
      "label": "Photo 3"
    },
    {
      "type": "image_picker",
      "id": "image_4",
      "label": "Photo 4"
    },
    {
      "type": "image_picker",
      "id": "image_5",
      "label": "Photo 5"
    }
  ],
  "presets": [
    {
      "name": "Elaris7 + You Social",
      "category": "Social Proof"
    }
  ]
}
{% endschema %}
